import "./loader.css";

const Loader = () => {
    return (
        <div className='flex items-center justify-center h-dvh w-screen bg-primary'>
            <div className='loader'></div>
        </div>
    );
};

export default Loader;
